# Copy this file to .env before running docker compose.
# Leave GH_TOKEN empty if you plan to authenticate once with --auth
# and persist the login state in ./copilot-data.

GH_TOKEN=
LOG_FILE=/var/log/copilot-api/copilot-api.log

# Optional proxy settings
HTTP_PROXY=
HTTPS_PROXY=
